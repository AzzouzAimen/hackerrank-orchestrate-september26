# Development semantic reference completion — 2026-09-12

The 14 development cases are now scoreable as semantic references. The approved, development-only file is `../extraction_artifacts/semantic_inventory_01/semantic_reference_annotations_dev_20260912_v4_frozen.json`; each case cites its exact saved request and SHA-256 in `development_runtime_inputs_v2/manifest.json`. Required meanings are written separately from canonical seven-type facts. Every case requires a nonempty fact set, preserves unknowns, lists harmful interpretations, and allows valid equivalent representations where evidence supports them. Approval is for benchmark reference use, not a claim of organizer ground truth or measured model accuracy.

| Gap | Resolution |
|---|---|
| user_10 QuickCrew payout | `cash_classification=pending_credit` targets the exact message phrase “next QuickCrew payout”; amount, currency, date, and event ID remain unknown. |
| user_26 approved invoice | `future_event_confirmation` targets the exact approved-invoice phrase. IDR 30780000 and expected 2025-08-15 come from that message. Actual settlement stays uncertain; unapproved invoices are excluded. |
| user_18 internal transfer | `lifecycle_relationship=internal_transfer` targets “matching debit and credit” with null `related_event_id` and unresolved event IDs. No unrelated raw events are treated as transfer legs. |
| user_23 verified prize | `cash_classification=pending_credit` targets “prize claim”; no prize currency, amount, date, or event ID is inferred. |
| user_14 childcare | `stream_status=ongoing` targets “new recurring childcare payment”; amount, exact start date, currency, and event ID stay unknown. The separately supported EUR salary resumption remains a separate fact. |

One backward-compatible schema extension was needed: an optional exact `source_target` quote for facts without a concrete event or structured stream, plus a nullable lifecycle parent ID only when the pair is explicitly unresolved. The seven fact types did not change. The extractor's contract sentence and saved request schema were updated mechanically for this targeting form. The finance consumer validates the quote and declines to apply an unidentified transfer pair; no financial arithmetic or recommendation rule was changed. Older facts remain valid.
Source-only facts with unknown event identity or monetary details are semantic coverage, not authorization to fabricate a cash flow or transfer exclusion in a recommendation.

The v3 empty references for user_12 and user_04 are corrected: supplied seasonal history supports `stream_status=ended`, and prior bonus history identifies the IDR bonus stream while the current amount/date remain unknown and contingent. User_16 keeps the 12% rent amendment separate from the scheduled outstanding balance. Direct inspection of image_02 confirms INR 100000 Balance Due and INR 100000 Amount Received; INR 200000 gross is not current due. User_20 requires the uncredited refund meaning without repeating linked raw amount/date, and user_229 requires the retry relationship without repeating failed/scheduled raw fields.

Offline validation: the input builder and frozen-reference regeneration match saved bytes; all 14 development cases pass Pydantic, evidence ownership, target/currency provenance, source/image hashes, exact payload hashes, and unknown-preservation checks. The full Python test suite passes **127/127**. The checks use no sample labels or profile currency for unrelated streams, and no holdout evidence, model/API call, or dataset mutation. No semantic scorer, prompt optimization, model inference, or full-dataset recommendation run was performed.
